import 'package:looper_app/model/current.dart';

class DiscoverCurrentsModel {
  String currentTitle;
  List<Current> currents;
    DiscoverCurrentsModel({
      required this.currentTitle,
      required this.currents
    });
}